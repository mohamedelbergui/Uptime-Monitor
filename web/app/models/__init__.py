# Import all database models here
# This ensures SQLAlchemy registers them properly when the package is loaded
# from app.models.user import User

# List models to allow clean 'from app.models import *' if needed
# __all__ = ["User"]